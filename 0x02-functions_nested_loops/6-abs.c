#include "main.h"
/**
* _abs - prints the absolute value of a number
*@ab: number targeted
*Return: returns 0
*/

int _abs(int ab)
{
if (ab > 0)
return (ab);
else if (ab < 0)
return (-ab);
else
return (0);
}
